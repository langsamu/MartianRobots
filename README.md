# MartianRobots

## CLI instructions

### Restore + build + run
```bat
dotnet run -p CLI < CLI\SampleInput.txt
```

### Run only
```bat
dotnet CLI\bin\Debug\netcoreapp2.2\CLI.dll < CLI\SampleInput.txt
```